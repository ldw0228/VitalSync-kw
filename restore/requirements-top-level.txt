# Portable top-level dependency snapshot captured 2026-08-30.
# Prefer `uv pip install --python .venv/bin/python -e '.[dev]'` first.
# These pins document the versions used for the recorded artifacts; platform-
# specific CUDA wheels may require the vendor index appropriate to the host.
matplotlib==3.11.1
numpy==2.5.2
openpyxl==3.1.5
pandas==3.0.5
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.18.1
snntorch==1.0.0
torch==2.13.0+cu130
tqdm==4.70.0
pytest==9.1.1
xgboost==3.4.1
