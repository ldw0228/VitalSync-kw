# Legacy compact ANN/SNN baseline

Repository-safe copy of the compact model from entry 017. Put the private NPZ below `data/`; output goes to `outputs/`. It is preserved for reproducibility and comparison, not presented as the current final architecture.

```powershell
python train_formal_cv.py
```
